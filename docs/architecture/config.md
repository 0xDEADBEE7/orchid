# Configuration resources

The active configuration model is documented in [NEW_CONFIG.md](NEW_CONFIG.md).

Use a complete, self-contained resource tree with `--config <DIR>`. It contains
`config.json`, `connections/`, `policies/`, `prompts/`, and `sessions/`.
There are no profile or legacy configuration aliases.
