# Meta execution

Meta/persona delegation is not part of the current clean configuration model.
If delegation is added later, it must use an explicitly resolved Policy,
Connection, and Prompt within the selected config directory.
