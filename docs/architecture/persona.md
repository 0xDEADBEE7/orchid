# Prompts

The former persona model has been replaced by named Prompt resources. See
[NEW_CONFIG.md](NEW_CONFIG.md) and the `prompts/` directory schema.

Select a policy prompt with `--prompt <name>` on `create` or `send`; prompt
references are retained in session metadata.
